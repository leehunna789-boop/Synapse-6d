import streamlit as st
import time

# ตั้งค่าหน้าแอป
st.set_page_config(page_title="SYNAPSE 6D", page_icon="💎", layout="centered")

st.title("💎 SYNAPSE : 6D ENERGY")
st.write("ระบบบำบัดระดับเซลล์ (Cellular Healing System)")

# ส่วนรับข้อมูล
mood = st.text_input("วันนี้คุณรู้สึกอย่างไร?", placeholder="เช่น เหนื่อย, เครียด, ต้องการพลัง...")

if st.button("ACTIVATE ENERGY"):
    if mood:
        st.write(f"กำลังปรับจูนคลื่นพลังงานสำหรับอาการ: {mood}")
        progress_bar = st.progress(0)
        for percent_complete in range(100):
            time.sleep(0.01)
            progress_bar.progress(percent_complete + 1)
        
        st.success("การปรับจูนเสร็จสิ้น! ฟังเสียงบำบัดด้านล่าง")
        # ใส่เสียงบำบัด (ตัวอย่างเสียงคลื่นความถี่)
        st.audio("https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3")
    else:
        st.warning("กรุณาพิมพ์ความรู้สึกของคุณก่อนครับ")
